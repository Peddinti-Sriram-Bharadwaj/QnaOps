---
- hosts: localhost
  connection: local
  tasks:
    - name: Apply nginx ConfigMap
      k8s:
        state: present
        definition: "{{ lookup('file', '../k8s/nginx-configmap.yaml') }}"

    - name: Apply fastapi deployment and service
      k8s:
        state: present
        definition: "{{ lookup('file', '../k8s/fastapi-deployment.yaml') }}"

    - name: Apply nginx deployment and service
      k8s:
        state: present
        definition: "{{ lookup('file', '../k8s/nginx-deployment.yaml') }}"
